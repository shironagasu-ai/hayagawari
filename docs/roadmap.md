# ロードマップ

## 着手順

| 順 | タスク | 状態 | メモ |
|---|---|---|---|
| 1 | トップに作例動画（背景ループ） | 実装済み | 方式 A: 書き出した MP4（H.264・音声なし）を背景で再生。イラスト 9 作品（作者名 HAYAGAWARI・サブタイトル PORTFOLIO・リンクは本番 URL）。README 冒頭にもプレビュー（アニメーション WebP） |
| 2 | ロゴのデザイン | 実装済み | 方向性 a: 文字を横に切って下半分をずらした形（早変わり＝場面の切り替え）。表示時に 1 字ずつ組み上がり、その後ときどき一瞬ずれる |
| 3 | A2 作業の保存 | 実装済み | 画像・入力・注目点・設定を IndexedDB に保存し、タブが破棄されても復元。保存は 1 件（直近の作業） |
| 4 | 演出の拡張 | 未着手 | 下記。これを v1.0.0 として出す（今回の版は v0.9.0） |

## 演出の拡張方針（v1.0.0）

**案 B（倍増）＋スタイル +3。** 増やしたあとは質を磨く（崩れる組み合わせを抑える）。

| カテゴリ | 現在 | 目標 |
|---|---|---|
| スタイル | 9 ＋ MIX | 12 ＋ MIX（例: RISO / CINEMA / ZINE） |
| オープニング | 8 | 16 |
| エンディング | 7 | 15 |
| 1 作品の見せ方 | 9 | 18 |
| 場面の切り替え | 9 | 15 |
| 背景の飾り | 6 | 12 |
| 配色の傾向 | 7 | 12 |

### 進め方（PR を分けて、プレビューの演出カタログで確認）

| PR | 内容 |
|---|---|
| ① 準備 | 演出カタログ（`#catalog`・本番でも公開）、演出ごとのファイル分割（`src/fx/`）、全演出の自動テスト、サンプル画像（Midjourney）の差し替え |
| ② | オープニング・エンディング（+16） |
| ③ | 見せ方（+9） |
| ④ | 切り替え・背景の飾り・配色（+17）と描画の部品（モザイク・ノイズで溶ける・ひし形ワイプ・網点など） |
| ⑤ 仕上げ | スタイル +3、出やすさの調整、合わない組み合わせの抑制、効果音の対応付け、作例動画の作り直し、v1.0.0 |

- 演出を足すときは `src/fx/<カテゴリ>.js` に実装し、`src/fx/labels.js` に表示名と説明を足す（テストが抜けを検出）
- 追加した演出は、E2E で「描ける（真っ黒・単色にならない）・説明がある・描画命令が多すぎない」を自動で確認する
- v1.0.0 では同じシードでも以前と違う映像になる（CHANGELOG に明記）

## 未決の小さな確認

- 作家名が空のとき「PORTFOLIO」を出すか
- MONO スタイルで作品自体もモノクロにするか
- 書き出した動画の利用責任について README に注記するか

## その後の候補

- C3 SNS 向けの長さプリセット（15 / 30 / 60 秒）
- C1 + C4 部分固定して振り直し、推し作品の強調
- A4 端末性能に合わせた画質の自動調整
- B1 BGM の持ち込み
