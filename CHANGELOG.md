# 更新履歴

このプロジェクトのバージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) に従います（決め方は [AGENTS.md](AGENTS.md) の「バージョン管理」）。
書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) にならっています。

## [Unreleased]

## [1.0.0] - 2026-09-26

演出を大幅に増やした版です。オープニング・エンディング・作品の見せ方・切り替え・背景の飾り・配色・スタイルがおよそ倍になり、ページ下部の「演出カタログ」で 1 つずつ見比べられます。

**同じシードでも以前（0.9.x）と違う映像になります。** 演出の候補と選び方が変わったためです。0.9.x で共有した URL を開くと、別の映像になります。保存した作業（画像・入力・設定）はそのまま読み込めます。**音はなくなりました**（詳しくは「変更」）。

### 追加
- スタイルを 3 種追加（RISO: リソグラフ風の 2 色刷り／CINEMA: 映画のような黒帯と青緑×橙／ZINE: コピー誌風のざらつき。計 12 種＋ミックス）。それぞれ専用の配色つき
- オープニングを 8 種追加（ブラインド・アイリス・マーキー・タイル・段階引き・対角・網点・スラント。計 16 種）
- エンディングを 8 種追加（コラージュ・バブル・クレジット・コンタクト・ズームアウト・パタパタ・ウォール・プリント。計 15 種）
- 作品の見せ方を 9 種追加（パンチイン・スキャン・スイング・ダイブ・ツートン・4 分割・スラム・シネマ・ピクセル。計 18 種）。スタイルごとの出やすさも設定
- 作品の切り替えを 6 種追加（ピクセル化・ノイズ溶解・網点・ひし形・ブラウン管・フリップ。計 15 種）
- 背景の飾りを 6 種追加（波紋・テロップ・図形・走査線・後光・網点グラデ。計 12 種）
- 配色を 8 種追加（同系色・補色・くすみ・青焼き・墨と朱、新スタイル用のリソ・シネマ・ジン。計 15 種）。これまで配色はスタイルごとに 1 種で固定だったが、スタイルごとの重みで選ぶように（同じスタイルの作品は同じ配色にそろえる）
- 演出カタログ（`#catalog`。ページ下部の「演出カタログ」から）：カテゴリごとに全演出を同じサンプル作品で並べて見比べられる。一覧のまま、画面中央のカード（PC ではマウスを乗せたカード）がその場で再生され、ほかのカードはコマ送りで動く。全画面での再生もできる

### 変更
- **音をなくした**：合成のビートと効果音、プレーヤーの 🔊 ボタン（M キー）、書き出しの音声の選択を削除し、書き出しは映像のみに。質の低い音で映像の印象を下げないため。好きな曲を持ち込んで、それに合わせた映像を作る機能を次の版（1.1）で予定
- 詳細設定のスタイル・オープニング・エンディングの選択肢を、演出の一覧から自動で作るように（演出を足すとボタンも増える）
- 作品の見せ方と合わない背景の飾り（例: ピクセルと網点、スキャンと走査線）は組み合わせないように
- トップの作例動画と README のプレビューを、サンプル画像 10 点と v1.0.0 の演出で作り直した
- 演出の実装をカテゴリごとのファイル（`src/fx/`）に分割（見た目は変わらない）
- 「サンプルで試す」を描画で作った図形から実際のイラスト 10 点に差し替え。読み込むたびに 6〜8 点をランダムに選ぶ。サンプルの注目点は見本として手で決めてある（注目点エディタの「自動検出に戻す」で解析結果にできる）。サンプル画像・作例動画のイラストは MIT の対象外（`assets/samples/NOTICE.md`）

### その他
- README を使う人向けの内容に絞り、開発の決まりごと・構成・リリースや公開の手順は `AGENTS.md` に移した

## [0.9.1] - 2026-09-26

### 変更
- 作品ごとのタイトルと注目点を、詳細設定の開閉に関係なくいつでも編集できるようにし、常に映像に反映するようにした（これまでは詳細設定を開いたときだけ表示・反映していたため、編集できないように見えていた）。詳細設定は映像全体の設定（テンポ・スタイル・並び順・オープニング・エンディング・シード）だけを扱う
- 再生画面から戻る操作を分かりやすく：左上に常に「← 戻る」を表示（操作していない間は薄くなるだけで押せる）。下の「✎ 編集」はなくした（E キーは従来どおり）。スマホの「戻る」（スワイプ・ブラウザの戻る）でも再生画面を閉じられる。操作が隠れているときの最初のタップは、止めずに操作を表示するだけにした

## [0.9.0] - 2026-09-26

1.0 に向けた準備版。トップページを作り直し、作業を保存できるようにしました（1.0.0 は演出を増やす次の版で出す予定）。

### 追加
- トップに作例動画（イラスト 9 作品を HAYAGAWARI で映像化したもの）を背景ループで表示。縦長の画面では縦動画
- ロゴを新しくデザイン（文字を横に切って下半分をずらした形。表示時に 1 字ずつ組み上がる）
- 作業の保存：画像・入力・シード・詳細設定・作品タイトル・手で直した注目点をブラウザ内に保存し、次に開いたときに復元
- 作品一覧の「すべて外す」
- サイトにバージョン表記、GitHub にタグとリリースを自動作成
- PR ごとのプレビュー（`/pr/<番号>/`。マージ前に実機で確認でき、URL は PR にコメントされる）
- README 冒頭に作例のプレビュー

## [0.6.2] - 2026-09-26

### 修正
- 別タブから戻ったとき、作品タイトルが作家名・サブタイトル・リンク欄にずれて入る（#8）

## [0.6.1] - 2026-09-26

### 修正
- 映像の冒頭で鳴るはずの効果音が鳴っていなかった（#7）

### その他
- GitHub Actions で E2E テスト（Chromium / Google Chrome）と、テストが通ったときだけ GitHub Pages へ公開（#7）

## [0.6.0] - 2026-09-25

### 追加
- 効果音とビートの合成（スタイルごとの音色）。プレーヤーと書き出しで オン / 効果音のみ / なし を選択（#6）

## [0.5.0] - 2026-09-25

### 追加
- 1 コマずつ描いて MP4 に書き出し（1080p / 4K・60 / 30fps。H.264 → VP9 → AV1 の順に使えるものを選ぶ）（#5）

## [0.4.0] - 2026-09-25

### 追加
- スタイル MIX（作品ごとにスタイルを抽選）と 4 つの新スタイル、オープニング・エンディングの追加（#4）

### 変更
- サブタイトルが空のとき「PORTFOLIO」を自動で入れない（#4）

## [0.3.0] - 2026-09-25

### 変更
- かんたん / こだわり の切り替えを「詳細設定」アコーディオンに置き換え。閉じている間はすべておまかせ（#3）

## [0.2.0] - 2026-09-25

### 追加
- オープニング・エンディングのパターン、振付の追加、注目点エディタ、かんたんモード（#2）

## [0.1.0] - 2026-09-25

### 追加
- 最初の版：イラストの注目点を解析し、シードごとに違う演出のポートフォリオ映像を生成（#1）

[Unreleased]: https://github.com/shironagasu-ai/hayagawari/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/shironagasu-ai/hayagawari/releases/tag/v1.0.0
[0.9.1]: https://github.com/shironagasu-ai/hayagawari/releases/tag/v0.9.1
[0.9.0]: https://github.com/shironagasu-ai/hayagawari/releases/tag/v0.9.0
